@using MusicPortal.Core
@model Artist
@{}

<h1>Top Albums</h1>
<div class="row">
    @foreach (Album album in Model.Albums)
    {
        <a class="pick_out_block" href="/Home/ArtistAlbum?artistName=@Model.Name&albumName=@album.Name">
            <div class="col-lg-2">
                <img class="img-rounded center-block" src="@album.PictureLink" />
                <p class="text-center">@album.Name</p>
            </div>
        </a>
    }
</div>